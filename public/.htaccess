Options -Indexes
RewriteEngine On

# Servir archivos y directorios reales tal cual (JS, CSS, imágenes, HTML)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Todo lo demás (rutas API y páginas) → index.php
RewriteRule ^(.*)$ index.php [QSA,L]